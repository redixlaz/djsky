
10 changes: 5 additions & 5 deletions 10
.github/workflows/tvipt.yml
@@ -1,5 +1,5 @@
# Thanks to LITUATUI user on github
# Thanks to LITUATUI user on github
name: update portugals
name: tvi-pt action
on:
on:
  schedule:
  schedule:
    - cron: "00 04,16 * * *"
    - cron: "00 04,16 * * *"
@@ -14,16 +14,16 @@ jobs:
      - name: checkout
      - name: checkout
        uses: actions/checkout@v3
        uses: actions/checkout@v3


      - name: update Portugals
      - name: updating tvi-pt
        run: sh ressources/tvipt/sh/tvipt.sh
        run: sh ressources/tvipt/sh/tvipt.sh


      - name: setup git config
      - name: setup git config
        run: |
        run: |
          git config user.name "portugals bot"
          git config user.name "tvi-pt bot"
          git config user.email "<>"
          git config user.email "<>"
      - name: commit
      - name: commit
        run: |
        run: |
          git pull
          git pull
          git add all.m3u
          git add all.m3u
          git commit -m "update stream"
          git commit -m "tvi-pt updated"
          git push origin master
          git push origin master
